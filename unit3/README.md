# LDD3 第三章源码案例
**本目录源码仅涉及LDD3第三章相关代码。**

示例代码并未完全按照第三章讲解所实现，未采用本章节中通过 `mknod` 手动创建设备文件的方法。考虑到在现代 Linux 驱动开发中（尤其是在实际项目中），通常通过在驱动中调用 `device_create()` 自动注册生成 `/dev/xxx` 设备节点，因此未编写与 `mknod` 相关的脚本，而是采用了当前主流的自动创建设备节点方式。

------

注：示例代码未考虑多并发场景（第五章才涉及并发），因此该字符设备被限制在同一时间只能被打开一次，否则可能出现并发问题。

